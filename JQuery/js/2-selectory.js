$( function() {
    // pobieranie elem po znaczniku
    var paragraphs = $('p');
    console.log(paragraphs);

    // pobieranie elem po id
    var paragraphOne = $('#paragraf');
    console.log(paragraphOne);

   // pobieranie elem po klasie
   var paragraphTwo = $('.paragraf');
   console.log(paragraphTwo); 
} );